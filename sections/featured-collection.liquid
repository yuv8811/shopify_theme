<<<<<<< HEAD
{% assign featured_collection = section.settings.collection %}

<section class="section">
  <div class="page-width">
    <div class="section-header{% if section.settings.center_text %} section-header--center{% endif %}">
      {% if section.settings.eyebrow != blank %}
        <p class="section-header__eyebrow">{{ section.settings.eyebrow }}</p>
      {% endif %}
      <h2>{{ section.settings.heading }}</h2>
      {% if section.settings.text != blank %}
        <p class="muted">{{ section.settings.text }}</p>
      {% endif %}
    </div>

    {% if featured_collection != blank and featured_collection.products_count > 0 %}
      <div class="product-grid" style="--columns: {{ section.settings.columns_desktop }};">
        {% for product in featured_collection.products limit: section.settings.products_to_show %}
          {% render 'product-card', product: product, show_vendor: section.settings.show_vendor, show_secondary_image: section.settings.show_secondary_image %}
        {% endfor %}
      </div>

      {% if section.settings.show_view_all %}
        <div class="button-row"{% if section.settings.center_text %} style="justify-content:center;"{% endif %}>
          <a class="btn btn--secondary" href="{{ featured_collection.url }}">{{ section.settings.view_all_label }}</a>
        </div>
      {% endif %}
    {% else %}
      <div class="empty-state">
        <h3>Select a collection</h3>
        <p>Add a collection in the theme editor to populate this product grid.</p>
      </div>
    {% endif %}
  </div>
</section>
=======
<div class="featured-collection-section page-width">
  {%- if section.settings.heading != blank -%}
    <h2 class="section-title text-center">{{ section.settings.heading }}</h2>
  {%- endif -%}

  {%- assign collection = collections[section.settings.collection] -%}
  {%- assign limit = section.settings.limit -%}

  <div class="product-grid">
    {%- if collection != blank and collection.all_products_count > 0 -%}
      {%- for product in collection.products limit: limit -%}
        <div class="product-card">
          <a href="{{ product.url }}" class="product-card__link">
            <div class="product-card__image-wrapper">
              {%- if product.featured_image -%}
                <img src="{{ product.featured_image | image_url: width: 300 }}" 
                     alt="{{ product.featured_image.alt | escape }}" 
                     loading="lazy" 
                     width="300"
                     height="{{ 300 | divided_by: product.featured_image.aspect_ratio | round }}">
              {%- else -%}
                {{ 'product-1' | placeholder_svg_tag: 'placeholder-svg' }}
              {%- endif -%}
            </div>
            <h3 class="product-card__title">{{ product.title }}</h3>
            <div class="product-card__price">
              <span>{{ product.price | money }}</span>
            </div>
          </a>
        </div>
      {%- endfor -%}
    {%- else -%}
      {%- for i in (1..limit) -%}
        <div class="product-card placeholder-card">
          <div class="product-card__image-wrapper">
            {{ 'product-' | append: i | placeholder_svg_tag: 'placeholder-svg' }}
          </div>
          <h3 class="product-card__title">Example Product Title</h3>
          <div class="product-card__price">
            <span>$19.99</span>
          </div>
        </div>
      {%- endfor -%}
    {%- endif -%}
  </div>
</div>
>>>>>>> 69e67f84842f7704d36389c377b46414a52de1d1

{% schema %}
{
  "name": "Featured Collection",
  "tag": "section",
  "class": "shopify-section-featured-collection",
  "settings": [
    {
      "type": "text",
<<<<<<< HEAD
      "id": "eyebrow",
      "default": "Featured selection",
      "label": "Eyebrow"
    },
    {
      "type": "text",
      "id": "heading",
      "default": "Merchandise your strongest assortment",
      "label": "Heading"
    },
    {
      "type": "textarea",
      "id": "text",
      "default": "A configurable product grid gives you campaign flexibility without changing templates.",
      "label": "Text"
    },
    {
=======
      "id": "heading",
      "default": "Featured Collection",
      "label": "Heading"
    },
    {
>>>>>>> 69e67f84842f7704d36389c377b46414a52de1d1
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    },
    {
      "type": "range",
<<<<<<< HEAD
      "id": "products_to_show",
      "min": 4,
      "max": 12,
      "step": 1,
      "default": 8,
      "label": "Products to show"
    },
    {
      "type": "range",
      "id": "columns_desktop",
      "min": 2,
      "max": 4,
      "step": 1,
      "default": 4,
      "label": "Desktop columns"
    },
    {
      "type": "checkbox",
      "id": "show_vendor",
      "label": "Show vendor",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "show_secondary_image",
      "label": "Show secondary image on hover",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_view_all",
      "label": "Show view all button",
      "default": true
    },
    {
      "type": "text",
      "id": "view_all_label",
      "label": "View all label",
      "default": "View full collection"
    },
    {
      "type": "checkbox",
      "id": "center_text",
      "label": "Center content",
      "default": true
=======
      "id": "limit",
      "min": 2,
      "max": 12,
      "step": 1,
      "default": 4,
      "label": "Products to Show"
>>>>>>> 69e67f84842f7704d36389c377b46414a52de1d1
    }
  ],
  "presets": [
    {
      "name": "Featured Collection"
    }
  ]
}
{% endschema %}
